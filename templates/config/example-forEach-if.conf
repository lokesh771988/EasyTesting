# Example: forEach (dropdown, table), if condition, getText, display, >> same-row
# 1) Dropdown: loop over options, if value matches then select
# 2) Table: display full table using forEach row
# 3) Table: find row where first column = "Apple", then click button in same row
# headless=false
# goto:https://example.com/page-with-table-and-dropdown
# wait:1000

# --- Example 1: Dropdown - get each option, if match then select ---
# forEach:select#mySelect option
# getText=>>=optVal
# display=$optVal
# if:$optVal=Second Option
# select=select#mySelect=label:Second Option
# endIf
# endForEach

# --- Example 2: Display full table (each row, each cell) ---
# forEach:table#data tr
# getText=>>td:nth-child(1)=col1
# getText=>>td:nth-child(2)=col2
# display=$col1
# display=$col2
# endForEach

# --- Example 3: Find row where first column = "Apple", click Edit in same row ---
# forEach:table#data tbody tr
# getText=>>td:nth-child(1)=cell1
# if:$cell1=Apple
# click=>>button.edit
# endIf
# endForEach
